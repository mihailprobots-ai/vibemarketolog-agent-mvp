# Results folder

This folder stores live VibeMarketolog Agent API run artifacts.

Run:

```bash
python backend/run_live_demo.py --repeat 2
```

The script creates `live_run_*.json` and `LIVE_RUNS.md` with generation IDs, model, cost and balance metadata.

Do not commit `.env` or API keys.
